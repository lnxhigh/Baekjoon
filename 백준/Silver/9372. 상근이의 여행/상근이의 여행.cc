#include <bits/stdc++.h>
using namespace std;

int T;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    cin >> T;
    while (T--) {
        int N, M;
        cin >> N >> M;
        for (int m = 0; m < M; m++) {
            int a, b;
            cin >> a >> b;
        }
        cout << N - 1 << "\n"; // ...
    }
    
    return 0;
}
