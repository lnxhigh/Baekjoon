#include <bits/stdc++.h>
using namespace std;

int N;
string str;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    
    while (true) {
        getline(cin, str);
        if (str == "") break;
        cout << str << endl;
    }
    
    return 0;
}
